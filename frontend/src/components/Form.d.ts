interface FormProps {
    onShortURLClick: (longURL: string) => void;
    isLoading?: boolean;
}
export declare const UrlForm: ({ onShortURLClick, isLoading }: FormProps) => import("react/jsx-runtime").JSX.Element;
export {};
